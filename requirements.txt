streamlit
replicate
pydantic>=2.0
python-dotenv
requests
openai
moviepy>=2.0.0
imageio-ffmpeg
numpy
pillow

# VCM Studio (FastAPI backend + SQLite par défaut ; Postgres managé en prod)
sqlmodel
psycopg[binary]>=3.2   # driver Postgres v3 (prod) ; SQLite reste le défaut local/CI
fastapi
uvicorn[standard]
httpx

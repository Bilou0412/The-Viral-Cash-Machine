streamlit
replicate
pydantic>=2.0
python-dotenv
requests
openai
moviepy>=2.0.0
imageio-ffmpeg
numpy
pillow

# VCM Studio (FastAPI backend + SQLite par défaut ; Postgres managé en prod)
sqlmodel
psycopg[binary]>=3.2   # driver Postgres v3 (prod) ; SQLite reste le défaut local/CI
fastapi
uvicorn[standard]
httpx
boto3   # backend object storage R2/S3 (import paresseux ; actif si STORAGE_BACKEND=r2)
argon2-cffi   # hachage des mots de passe (auth, Phase B)
itsdangerous  # cookies de session signés (Starlette SessionMiddleware)
cryptography  # chiffrement des clés API par-utilisateur (Fernet, Phase B.2)
